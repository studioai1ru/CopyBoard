{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Permissions for the CopyBoard desktop window",
  "windows": ["main", "quick-access"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
