# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "ClipBoard"

[setup]
script = ""

[[actions]]
name = "Start App"
icon = "run"
command = "npm run dev; $code = $LASTEXITCODE; if ($code -ne 0) { Write-Host 'App exited with an error.' -ForegroundColor Red } else { Write-Host 'App stopped.' -ForegroundColor Green }; Write-Host 'This terminal will close in 30 seconds.'; Start-Sleep -Seconds 30; exit $code"

[[actions]]
name = "Publish New Version"
icon = "tool"
command = "npm version patch; $code = $LASTEXITCODE; if ($code -eq 0) { git push origin main --follow-tags; $code = $LASTEXITCODE }; if ($code -eq 0) { Write-Host 'Version and tag were pushed. GitHub installer builds have started.' -ForegroundColor Green } else { Write-Host 'Release failed. Read the error above.' -ForegroundColor Red }; Write-Host 'This terminal will close in 30 seconds.'; Start-Sleep -Seconds 30; exit $code"
